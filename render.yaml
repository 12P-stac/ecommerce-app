services:
  - type: web
    name: ecommerce-app
    env: docker
    plan: free
    branch: main
    region: oregon
    buildCommand: "./mvnw clean package -DskipTests"
    startCommand: "java -jar target/*.jar"
    autoDeploy: true
